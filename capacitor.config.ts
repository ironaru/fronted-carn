import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Comite Racho del Norte',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
